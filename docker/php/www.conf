; Development container only. Docker Desktop maps bind-mounted files to root
; inside the container, so FPM workers must run as root to write storage/ and
; bootstrap/cache. A production image would run as an unprivileged user with
; the application code baked in rather than bind-mounted.
[www]
user = root
group = root
listen.owner = root
listen.group = root
